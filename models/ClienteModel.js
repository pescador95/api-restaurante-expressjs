import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

function cliente() {
  (nomeCliente = String),
    (cpf = String),
    (telefone = String),
    (dataNascimento = Date),
    (endereco = String);
}
